# This specifies the base Ubuntu system
BASEIMAGE_URL=https://dl.armbian.com/pine64/Buster_current
BASEIMAGE=armbian_pine64_current_$(date --iso-8601).7z

function decompress {
	7zr e $BASEIMAGE -so *.img > $TARGETIMAGE
}
